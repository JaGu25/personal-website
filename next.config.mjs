/** @type {import('next').NextConfig} */
// eslint-disable-next-line @typescript-eslint/no-var-requires
import { i18n } from './next-i18next.config.mjs'; // Importar la configuración de next-i18next

const nextConfig = {
    i18n
};

export default nextConfig;
